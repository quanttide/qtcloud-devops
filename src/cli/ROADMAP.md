# ROADMAP — qtcloud-devops-cli

## v0.8.0（已完成）

- [x] **PATH mock 工具** — `with_mock_env` / `mock_script` / `mock_custom` / `mock_not_found`
- [x] **`build status` 编排层测试** — gh 未安装/空数组/success/failure/cancelled/未知，cargo check 成功/失败，无 manifest 跳过
- [x] **`test status` 编排层测试** — cargo test 成功/失败，空目录
- [x] **`release status` 编排层测试** — 有标签/无标签/非 git 目录
