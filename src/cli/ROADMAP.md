# ROADMAP — qtcloud-devops-cli

## v0.8.0（计划）

- [ ] **PATH mock 工具** — `tests/mock/` 共享工具函数（`with_mock_env`、`mock_script` 等）
- [ ] **`build status` 编排层测试** — gh/cargo/git 各场景（成功/失败/未安装/异常输出）
- [ ] **`test status` 编排层测试** — cargo/python/go 等命令各场景
- [ ] **`release status` / `release publish` 编排层测试** — gh release/git tag 各场景
